{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "cd frontend-v2 && npm install && npm run build",
  "outputDirectory": "frontend-v2/dist",
  "functions": {
    "api/**/*.py": {
      "maxDuration": 60,
      "includeFiles": "{frontend-v2/dist/**,fonts/**,migrations/**,fixtures/**}",
      "excludeFiles": "{.env*,.git/**,_archive/**,frontend/**,frontend-v2/node_modules/**,frontend-v2/src/**,artifacts/**,cache/**,tests/**,Documents/**,*.png,*.zip,*.rar,*.db,*.sqlite3,mail-data/**}"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.py" }
  ]
}
