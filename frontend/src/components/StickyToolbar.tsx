import { Send, X } from 'lucide-react';
interface Props { count: number; onPrepare: () => void; onClear: () => void }
export function StickyToolbar({ count, onPrepare, onClear }: Props) { if (!count) return null; return <div className="fixed bottom-6 left-1/2 lg:left-[calc(50%+124px)] z-30 flex -translate-x-1/2 items-center gap-3 rounded-xl border border-ink-200 bg-white px-4 py-3 shadow-float"><b className="text-xs text-ink-700">{count} выбрано</b><button onClick={onPrepare} className="inline-flex items-center gap-1.5 rounded-lg bg-accent-600 px-3 py-1.5 text-xs font-semibold text-white"><Send className="h-3.5 w-3.5" />Подготовить запрос</button><button onClick={onClear} className="inline-flex items-center gap-1 rounded px-2 py-1.5 text-xs text-ink-500"><X className="h-3.5 w-3.5" />Снять выбор</button></div>; }
