# TASK-SERVER-START-20260831

## Сделано

SupplyDesk запущен на `http://127.0.0.1:8000/` и оставлен работающим.
Процесс запущен с `MAIL_OUTGOING_DISABLED=1`; durable-переключатель исходящей
почты в базе остаётся `0`.

## Проверено

- `GET /` → `200`.
- `GET /api/auth/me` → `200`.
- `GET /api/requests` без сессии → `401`.
- `GET /api/does-not-exist` → `404`.
- SQLite read-only: `outgoing_enabled=0`, `integrity_check=ok`.

## Ограничения

Проверены HTTP-доступность и безопасное состояние исходящей почты. Авторизация
пользователя и визуальная работа интерфейса в этом запуске отдельно не
проверялись.
