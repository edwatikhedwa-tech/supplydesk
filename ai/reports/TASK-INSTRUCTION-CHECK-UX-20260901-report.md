# TASK-INSTRUCTION-CHECK-UX-20260901

- Дата: `2026-09-01T06:38:31Z`
- Scope: сделать финальную проверку правил понятной владельцу проекта.
- Non-goals: application code, UI, API, database, mail, runtime settings,
  external services and unrelated worktree files.

## Сделано

Проблема была в шаблоне в корневом `AGENTS.md`: он показывал английские
названия и все возможные варианты одновременно, например
`PASS / NOT VERIFIED / BLOCKED`. Это выглядело как незаполненная форма и не
объясняло, что произошло именно в текущем ответе.

Изменения:

- `AGENTS.md` теперь требует русский блок `[ПРОВЕРКА ПРАВИЛ]`.
- В готовом ответе разрешено показывать только один фактический результат на
  строку и короткое объяснение простыми словами.
- `ai/AI_CONTRACT.md` синхронизирован с этим правилом.
- Chronology/state/handoff обновлены по правилам проекта; существующие записи
  в `ai/DECISIONS.md` не затрагивались, потому что файл уже был изменён до
  начала этой задачи.

## Простыми словами

Вместо списка вроде «да / нет / неизвестно» в конце будет написано только то,
что реально произошло: например «Коммит: не создавался — изменений для
сохранения не было». Основное объяснение результата остаётся в начале ответа;
служебный блок теперь только кратко подтверждает соблюдение правил.

## Что изменено и что не изменено

Изменены только инструкции и документы состояния проекта. Код SupplyDesk,
frontend, API, SQLite, письма, настройки отправки и существующие чужие файлы
рабочей папки не изменялись.

Перед изменением сохранены резервные копии:

`C:\Users\edwat\AppData\Local\Temp\supplydesk-instructions-backup-20260901\AGENTS.md`

`C:\Users\edwat\AppData\Local\Temp\supplydesk-instructions-backup-20260901\AI_CONTRACT.md`

## Что проверено

- `rg` подтвердил источник старого блока: корневой `AGENTS.md`.
- Прочитаны обязательные инструкции проекта и состояние Git.
- `GET http://127.0.0.1:8000/` вернул `200` — сервер отвечает.
- `GET http://127.0.0.1:8000/api/auth/me` вернул `200` — endpoint доступен.
- Неизвестный API вернул `404` — ошибка обрабатывается ожидаемым образом.
- Проверен diff: правка ограничена правилами и state-документами.

## Что не проверено

Нельзя заранее автоматически доказать, как будущий ответ агента будет выглядеть
в каждом случае. Это проверяется на следующих сообщениях. Сборка frontend и
визуальный smoke-тест не запускались, потому что интерфейс и application code
не менялись.

## Риски и откат

Риск низкий: меняется только текст инструкций. Если потребуется откат,
восстановите два файла из указанной резервной папки или отмените изменения
этого Task-ID в Git; старые пользовательские и рабочие файлы затронуты не
были.

## Уровень уверенности

Высокий для источника проблемы и состава изменений; средний для поведения
будущих ответов, потому что оно зависит от последующего применения правила.

## Закрытие

До финального closeout commit не создан, push не выполнялся. Точный commit и
итоговый статус будут указаны после проверки валидатора.
