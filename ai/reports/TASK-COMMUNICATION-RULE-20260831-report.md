# TASK-COMMUNICATION-RULE-20260831

- Дата: `2026-08-31`
- Scope: формат пользовательских ответов Codex в этом проекте.
- Non-goals: код SupplyDesk, API, база, письма и настройки приложения.

## Сделано

Добавлено правило в общий контракт `ai/AI_CONTRACT.md` и адаптер Codex
`AGENTS.md`:

1. существенные ответы пишутся кратко и на понятном русском языке;
2. в начале идут блоки `Сделано`, `Проблемы и ограничения`, `Следующий шаг`;
3. технические термины, команды, статусы `PASS`/`FAIL` и хеши коммитов
   сопровождаются объяснением человеческим языком;
4. служебный блок проверки инструкций остаётся в конце, но больше не заменяет
   нормальное объяснение результата.

Перед изменением сохранены резервные копии в
`Temp/instructions-backup-20260831/`.

## Проверено

- `python ai/tools/validate_state.py` — `PASS`: структура state-файлов проекта
  корректна.
- Изменены только инструкции и отчёт; application code не затронут.

## Не проверено

Формат нельзя проверить автоматическим тестом как поведение интерфейса; он
будет применяться в следующих ответах. Существующие изменения других задач не
перезаписывались.

## Следующий шаг

В следующих существенных ответах давать короткое объяснение по этой схеме и
оставлять только те технические детали, которые помогают принять решение.
