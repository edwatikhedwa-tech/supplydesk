# Page dependency tree

## `/supplier_finder.html` — Supplier Finder

Entry: `supplier_finder.html`

Dependencies:
- `supplier_finder.html`
  - Embedded CSS tokens and layout selectors
  - Embedded `DATA` fixture with positions and suppliers
  - `renderPositions()`
  - `renderFilters()`
  - `cardHtml(s)`
  - `render()`
  - filter event handlers
  - selection and export dialog handlers
  - toast and undo behavior
- External font stylesheet: Google Fonts (`Golos Text`, `IBM Plex Sans`, `IBM Plex Mono`)

No local imports, framework components, or API dependencies.
