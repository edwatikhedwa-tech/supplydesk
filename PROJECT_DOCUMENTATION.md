# Supplydesk — документация проекта

Актуальное описание текущей версии проекта поиска поставщиков. Документ дополняет существующий [README.md](README.md) и подробную документацию почтовой интеграции в [docs/mail-integration.md](docs/mail-integration.md).

**Это единственный главный документ проекта.** Остальные `docs/*.md` — не конкурирующие описания, а глубокие разборы отдельных подсистем, на которые этот документ ссылается. Правило с 25 августа 2026: любое изменение в коде сопровождается правкой этого файла (актуальные факты выше + запись в разделе 18 «Последняя проверка проекта»), детали конкретной подсистемы — в соответствующем `docs/*.md`.

**Обновлено:** 25 августа 2026 года.

Документ описывает фактическую реализацию в репозитории, а не целевую архитектуру из BRD. Если функция указана в разделе «Ограничения текущей версии», её нельзя считать готовой только потому, что она предусмотрена в требованиях.

### Как читать проект

- `README.md` — исходная документация PoC-пайплайна поиска, email и ИНН;
- `PROJECT_DOCUMENTATION.md` — **главный документ**, карта всего продукта и инструкция для запуска/поддержки;
- `docs/mail-integration.md` — расширенная инструкция именно по OAuth, SMTP, IMAP и почтовой БД;
- `docs/enrichment-and-cache.md` — краулер, LLM-ступень, веб-поиск-фоллбэк, реестр Checko и контроль стоимости;
- `docs/suppliers-screen.md` — экран «Поставщики» (карточка компании по ИНН, статусы отношений, обнаружение bounce);
- `DESIGN.md` — визуальные и UX-принципы интерфейса;
- код и миграции — источник истины при расхождении с текстом.

## 1. Назначение проекта

Supplydesk помогает снабженцу:

1. создать заявку с одной или несколькими позициями;
2. найти поставщиков по каждой позиции через XMLRiver и Яндекс;
3. проверить найденные компании и их контакты;
4. выбрать поставщиков для контакта;
5. подключить рабочую Яндекс.Почту через OAuth;
6. подготовить и отправить персонализированные запросы;
7. хранить исходящие и входящие сообщения в отдельных тредах по связке «заявка + поставщик».

Главный пользовательский сценарий:

```text
Заявка → отдельные позиции → поиск поставщиков → проверка → подготовка писем → очередь → переписка
```

Важно: кнопка отправки письма запускает реальную отправку. Для проверки интерфейса можно создать заявку и запустить поиск, но не нажимать «Отправить выбранным».

## 2. Текущее состояние

В проекте уже есть:

- вход через аккаунт Яндекса; локальный вход по паролю оставлен резервным для разработки;
- выход из кабинета и раздача страницы без данных для неавторизованного посетителя;
- **frontend — React SPA** (`frontend/`, см. раздел 3): рабочий стол, заявки, поставщики (CRM-картотека по ИНН), переписка и чёрный список;
- позиции заявки задаются одним полем — наименованием: позиция это поисковый запрос, количество в поиске не участвует;
- фоновый поиск поставщиков по позициям, глубина настраивается `SERP_PAGES`, обрабатываются **все** найденные сайты без искусственного потолка (`SERP_RESULTS_PER_POSITION` по умолчанию пуст — правило «сколько нашли, столько и обрабатываем»);
- **реальное извлечение контактов подключено к поиску** (не только CLI): краулинг сайта → регулярка на email/ИНН → LLM-фоллбэк на неоднозначных случаях → веб-поиск-фоллбэк (XMLRiver+Яндекс) для сайтов, недостижимых для краулера (антибот/бан/DNS-сбой) → сверка с реестром Checko. Подробности, актуальные цифры и защита от ложной привязки ИНН — в [docs/enrichment-and-cache.md](docs/enrichment-and-cache.md);
- сохранение поставщиков и связей «поставщик — позиция», плюс отдельная CRM-картотека `global_suppliers` по ИНН (одна карточка на компанию, даже если её нашли на двух доменах) — см. [docs/suppliers-screen.md](docs/suppliers-screen.md);
- Yandex OAuth, refresh token и SMTP XOAUTH2;
- шифрование OAuth-токенов на сервере;
- отправка одному или нескольким поставщикам отдельными письмами;
- очередь писем с ограничением параллелизма, retry и exponential backoff;
- IMAP XOAUTH2-синхронизация входящих сообщений;
- сопоставление входящих сообщений с тредами, включая автоматическое распознавание жёсткого bounce (см. [docs/suppliers-screen.md](docs/suppliers-screen.md) раздел 4);
- отдельный список неразобранных входящих;
- показ писем в исходной вёрстке: санитизация по белому списку плюс изолированный iframe, внешние картинки заблокированы;
- локальное SQLite-хранилище и PostgreSQL/Neon для production;
- адаптер Vercel Functions.

### Демонстрационная заявка и реальные заявки

Старый `supplier_finder.html` (с константой `DATA`) архивирован в `_archive/frontend-legacy/` после перехода на React SPA. Backend при инициализации по-прежнему синхронизирует демонстрационный каталог для заявки №1043 (теперь через фикстуру в коде, не через HTML-файл), поэтому заявка «Строительные материалы» доступна сразу после первого локального входа.

Для заявки №1043 endpoint запуска поиска намеренно возвращает уже готовый fixture и не делает новый внешний запрос. Для всех новых заявок поиск действительно обращается к XMLRiver, если в окружении заполнены `XMLRIVER_USER` и `XMLRIVER_KEY`, и полный конвейер обогащения (см. выше) реально извлекает контакты — это больше не CLI-only функциональность.

Это разделение по-прежнему важно при тестировании:

- №1043 — стабильные демонстрационные данные/fixture;
- новая заявка — реальный pipeline поиска, обогащения и сохранения результатов;
- наличие поставщика в fixture не означает, что он будет найден текущим внешним поиском в тех же деталях.

## 3. Архитектура

### Frontend

Frontend — React SPA в `frontend/` (Vite + TypeScript + Tailwind), собирается в `frontend/dist/` и раздаётся тем же Python backend'ом (заменил старый монолитный `supplier_finder.html`, который архивирован в `_archive/frontend-legacy/`, не удалён). Никаких сторонних BaaS (Supabase и т.п.) — весь фронтенд ходит только в `/api/*` этого backend'а.

Клиентский роутинг — `react-router` (`BrowserRouter`), backend отдаёт `frontend/dist/index.html` на любой несовпавший с `/api/*`/`/oauth/*` GET-путь (SPA fallback).

Основные разделы интерфейса:

- `Рабочий стол` — сводка и быстрые действия;
- `Мои заявки` — список и статусы заявок;
- `Поставщики` — CRM-картотека по ИНН: список с фильтрами/поиском/массовым выбором и карточка компании (статус отношений, контакты, статистика, история заявок, «сообщить о проблеме») — подробности в [docs/suppliers-screen.md](docs/suppliers-screen.md);
- `Переписка` — треды, входящие и исходящие сообщения;
- `Чёрный список` — исключённые компании;
- `Настройки · Почта` — подключение и проверка Яндекс.Почты.

### Backend

Backend — Python без web-фреймворка:

- [`supplier_app.py`](supplier_app.py) — локальный HTTP-сервер, маршрутизация API, сессии, OAuth callback и запуск фоновых задач;
- [`api/index.py`](api/index.py) — адаптер для Vercel Functions;
- `http.server.ThreadingHTTPServer` — локальный сервер;
- `ThreadingHTTPServer` и отдельные daemon-потоки — фоновые операции поиска;
- `MailQueue` — очередь отправки писем.

ORM не используется. Доступ к данным реализован через небольшой DB-API-репозиторий [`mail/repository.py`](mail/repository.py), который поддерживает SQLite и PostgreSQL.

### Что происходит при старте

Локальный запуск проходит в таком порядке:

```text
supplier_app.py
  → load_dotenv(.env)
  → Config.from_env()
  → MailRepository.ensure_schema()
  → seed_user() и создание workspace
  → seed_fixture_catalog() для демонстрационной заявки №1043
  → MailService + MailQueue
  → HTTP-сервер на APP_HOST:PORT
```

Особенности инициализации:

1. значения, уже заданные в окружении процесса, имеют приоритет над `.env`;
2. миграции `migrations/*.sql` применяются при каждом старте идемпотентно;
3. jobs и сообщения, оставшиеся в состоянии `sending` после остановки процесса, возвращаются в `queued`;
4. локальная очередь стартует вместе с сервером;
5. если `DATABASE_URL` задан, используется PostgreSQL, иначе создаётся SQLite-файл по `MAIL_DB_PATH`.

На Vercel последовательность почти такая же: `api/index.py` создаёт один экземпляр `SupplierApp`, запускает очередь для текущего тёплого serverless-инстанса и передаёт запросы в тот же `SupplierHandler`.

### Почтовая абстракция

Провайдер почты отделён от бизнес-логики:

```text
mail/providers/base.py       общий контракт провайдера
mail/providers/yandex.py     OAuth, SMTP и IMAP для Яндекс.Почты
mail/service.py               шаблоны, персонализация и бизнес-правила
mail/queue.py                 очередь и retry
mail/repository.py            сохранение аккаунтов, тредов и сообщений
```

Новый Gmail или Outlook-провайдер должен реализовать тот же контракт, не меняя модели тредов и сообщений.

## 4. Структура проекта

| Путь | Назначение |
|---|---|
| `frontend/` | React SPA (Vite+TS+Tailwind), исходники и сборка `frontend/dist/` |
| `_archive/frontend-legacy/` | архив старого монолитного `supplier_finder.html` (не удалён, не используется) |
| `supplier_app.py` | локальный backend, API, маршрутизация, обогащение поставщиков |
| `api/index.py` | запуск того же backend на Vercel |
| `mail/auth.py` | хеширование session/CSRF/OAuth значений |
| `mail/crypto.py` | шифрование OAuth-токенов AES-256-GCM |
| `mail/providers/base.py` | контракт почтового провайдера |
| `mail/providers/yandex.py` | Yandex OAuth, SMTP XOAUTH2 и IMAP XOAUTH2 |
| `mail/service.py` | шаблоны, персонализация, валидация, постановка в очередь |
| `mail/queue.py` | workers, retry, backoff и обработка jobs |
| `mail/repository.py` | SQL-репозиторий для SQLite/PostgreSQL, агрегация CRM-карточек поставщиков |
| `mail/content.py` | очистка HTML/text содержимого писем |
| `mail/bounce.py` | распознавание жёсткого/мягкого bounce во входящих письмах |
| `serp_parser.py` | сбор выдачи XMLRiver и дедупликация сайтов |
| `xmlriver_client.py` | HTTP-клиент XMLRiver с retry |
| `contact_crawler.py` | обход сайта поставщика с ретраями и различением blocked/rate_limited/unreachable |
| `collect_contacts.py` | сбор email с сайтов (CLI) |
| `collect_inn.py` | извлечение и проверка ИНН (CLI) |
| `email_extractor.py` / `inn_extractor.py` | регулярки и эвристики извлечения email/ИНН из HTML |
| `llm_fallback.py` | LLM-ступень (RouterAI) на страницах, где регулярка не справилась |
| `web_lookup.py` | веб-поиск-фоллбэк (XMLRiver+LLM) для сайтов, недостижимых для краулера |
| `checko_client.py` | сверка ИНН с реестром ЕГРЮЛ + финансовая отчётность (Checko, 100 бесплатных запросов/сутки на ключ, поддержана ротация нескольких ключей) |
| `verify.py` | верификация email и защита от ложной привязки ИНН к чужому сайту |
| `dadata_client.py` | подтверждение ИНН в реестре |
| `stop_domains.txt` | домены маркетплейсов, исключаемые из выдачи перед обогащением |
| `migrations/001_mail_integration.sql` | пользователи, mail accounts, треды, сообщения и jobs |
| `migrations/002_dashboard.sql` | заявки, позиции, профили поставщиков и blacklist |
| `migrations/003_mail_sync.sql` | UID-курсор IMAP-синхронизации |
| `migrations/004_unmatched_inbox.sql` | неразобранные входящие сообщения |
| `migrations/005_oauth_login.sql` | state-таблица для входа через Яндекс (до появления сессии) |
| `migrations/006_inbox_reply.sql` | ответ прямо из неразобранных входящих |
| `migrations/007_global_suppliers.sql` | CRM-картотека поставщиков по ИНН, статусы отношений, проблемы, оценки сделок |
| `migrations/008_supplier_registry.sql` | реестровые факты Checko (ОГРН, статус, дата регистрации) для карточки поставщика |
| `migrations/009_supplier_finances.sql` | выручка и чистая прибыль за последний отчётный год (Checko `/v2/finances`) |
| `migrations/010_supplier_blacklist_reason.sql` | причина и дата занесения в чёрный список для CRM-карточки |
| `frontend/src/components/suppliers/GlobalSupplierTable.tsx` | общая таблица для `/suppliers` и `/blacklist` (проп `view`) |
| `tests/` | unit/integration-тесты backend и mail flow |
| `docs/mail-integration.md` | подробная инструкция по Яндекс.Почте |
| `docs/enrichment-and-cache.md` | краулер, LLM, веб-поиск-фоллбэк, Checko, контроль стоимости |
| `docs/suppliers-screen.md` | экран «Поставщики», CRM-модель, bounce-детекция |
| `DESIGN.md` | визуальное направление, типографика, цвета и UX-принципы |
| `.env.example` | перечень переменных окружения без секретов |
| `vercel.json` | маршрутизация и параметры Vercel Functions |
| `requirements.txt` | Python-зависимости runtime и парсера |

## 5. Локальный запуск

### Требования

- Windows, macOS или Linux;
- Python 3.11+;
- доступ к XMLRiver для реального поиска;
- PostgreSQL только для production — локально достаточно SQLite.

### Установка

PowerShell:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
Copy-Item .env.example .env
```

Заполните `.env`. Реальный файл `.env` нельзя коммитить или пересылать.

### Минимальный запуск интерфейса

```powershell
python supplier_app.py
```

Откройте:

```text
http://127.0.0.1:8000/supplier_finder.html
```

Если порт занят:

```powershell
$env:PORT = "8765"
python supplier_app.py
```

Тогда адрес будет `http://127.0.0.1:8765/supplier_finder.html`.

### Вход

Основной способ — кнопка «Войти с помощью Яндекса» на экране входа. Для локального запуска нужно **отдельное** приложение Яндекс OAuth с Redirect URI `http://127.0.0.1:8000/oauth/yandex/callback`: у приложения одно поле Redirect URI, поэтому боевое приложение переиспользовать нельзя, не сломав вход на сайте. Его `YANDEX_CLIENT_ID` и `YANDEX_CLIENT_SECRET` прописываются в локальный `.env`.

Пользователь и рабочее пространство создаются автоматически при первом входе.

Резервный вход по паролю остаётся для случаев, когда OAuth недоступен — например, при работе без интернета. Он спрятан на экране входа под «Локальный вход для разработки» и использует:

```text
APP_USER_EMAIL=
APP_USER_PASSWORD=
```

Ролей, приглашений и разделения прав внутри рабочего пространства пока нет.

## 6. Переменные окружения

Все значения ниже должны храниться только в `.env` локально или в Vercel Environment Variables.

| Переменная | Обязательность | Назначение |
|---|---:|---|
| `XMLRIVER_USER` | для реального поиска | ID пользователя XMLRiver |
| `XMLRIVER_KEY` | для реального поиска | ключ XMLRiver |
| `APP_HOST` | нет | адрес локального сервера, обычно `127.0.0.1` |
| `PORT` | нет | порт, по умолчанию `8000` |
| `APP_BASE_URL` | да для OAuth | базовый URL приложения |
| `APP_USER_EMAIL` | для резервного локального входа | email пользователя разработки |
| `APP_USER_PASSWORD` | для резервного локального входа | пароль пользователя разработки |
| `MAIL_DB_PATH` | локально | путь к SQLite-файлу |
| `DATABASE_URL` | production | PostgreSQL/Neon connection string |
| `MAIL_TOKEN_ENCRYPTION_KEY` | для OAuth | ключ AES-256-GCM шифрования токенов |
| `MAIL_QUEUE_CONCURRENCY` | нет | параллелизм очереди, backend ограничивает максимум до 4 |
| `MAIL_MAX_RETRIES` | нет | максимум повторов, backend ограничивает максимум до 8 |
| `MAIL_DAILY_RECIPIENT_LIMIT` | нет | дневной лимит получателей |
| `YANDEX_CLIENT_ID` | да | Client ID приложения Яндекс OAuth — нужен и для входа, и для почты |
| `YANDEX_CLIENT_SECRET` | да | Client Secret приложения Яндекс OAuth |
| `YANDEX_REDIRECT_URI` | да | callback URI, должен совпадать с настройкой приложения буквально |
| `YANDEX_OAUTH_SCOPE` | нет | по умолчанию `mail:smtp mail:imap_full login:email` |
| `DADATA_TOKEN` | для проверки ИНН | токен DaData |
| `ANTHROPIC_API_KEY` | только для `--llm` | необязательный LLM fallback |
| `ROUTERAI_KEY` | для LLM-ступени | ключ RouterAI, см. [docs/enrichment-and-cache.md](docs/enrichment-and-cache.md) |
| `CHECKO_KEY` | для данных ЕГРЮЛ | ключ Checko: название, статус, ОКВЭД, контакты, риски |

У локального запуска и production разные приложения Яндекс OAuth, потому что поле Redirect URI одно на приложение: локальное смотрит на `http://127.0.0.1:8000/oauth/yandex/callback`, боевое — на адрес Vercel. Менять Redirect URI боевого приложения ради локальной отладки нельзя: это ломает вход на сайте.

Сгенерировать ключ шифрования можно так:

```powershell
python -c "from mail.crypto import generate_key; print(generate_key())"
```

В репозитории не должны находиться реальные пароли, API-ключи, OAuth-токены или содержимое `.env`.

## 7. Жизненный цикл заявки

### Создание

В форме «Новая заявка» каждая позиция добавляется отдельной строкой. У строки есть собственные плейсхолдеры:

- `Например, Кирпич М150`;
- `Например, 12 000 шт`.

Frontend отправляет backend массив объектов:

```json
{
  "name": "Труба ПВХ 110 мм",
  "quantity": "100 м"
}
```

Backend сохраняет строки в `request_positions` с ключами `p1`, `p2` и т. д. Старый формат свободного текста с разделителем `|` временно поддерживается для обратной совместимости API.

Одновременно создаются:

- строка в `requests` — название, описание, отправитель и компания;
- строка в `request_meta` — `draft`, прогресс поиска и возможная ошибка;
- строки в `request_positions` — нормализованные позиции;
- запись `audit_events` — действие `request.created`.

До запуска поиска поставщики не создаются и письма не ставятся в очередь.

### Поиск

1. Пользователь нажимает «Начать поиск».
2. Backend меняет статус заявки на `searching`.
3. Фоновый worker обрабатывает позиции по очереди.
4. Для каждой позиции выполняется запрос XMLRiver с суффиксом `купить`.
5. Результаты дедуплицируются по host.
6. До 30 результатов каждой позиции сохраняются в БД.
7. Поставщики связываются с позициями в `request_suppliers`.
8. После завершения статус меняется на `completed`.

Каждый новый результат обновляет/создаёт запись в `suppliers`, профиль в `supplier_profiles` и связь в `request_suppliers`. Если один и тот же host найден по двум позициям, компания хранится один раз, а список покрываемых позиций хранится в связи заявки.

Статусы заявки:

```text
draft → searching → completed
                  ↘ error
```

`POST /api/requests/{id}/search` обычно отвечает `202` сразу после постановки задачи. UI затем повторно загружает список заявок и видит итоговый статус. В одном процессе одновременно разрешён только один search worker; повторный запуск другой заявки до его завершения вернёт понятную ошибку.

В текущем интерфейсе после `completed` показывается: «Готово к отправке запросов». Это означает, что поставщики найдены, но письма ещё не отправлялись.

### Отправка

Отправка не выполняется автоматически после поиска. Пользователь сам выбирает поставщиков и нажимает «Написать» или «Отправить выбранным».

Для каждого поставщика создаются:

- отдельное письмо;
- отдельная job в `mail_jobs`;
- отдельный тред в `mail_threads`;
- отдельный статус в `request_supplier_states`.

CC/BCC для массовой отправки не используется.

Статусы поставщика:

```text
not_sent → queued → sending → sent
                         ↘ failed
```

Временная ошибка провайдера переводит job обратно в `queued` с ограниченным retry и увеличенной задержкой. Постоянная ошибка становится `failed` и отображается пользователю.

Очередь ограничена параметрами `MAIL_QUEUE_CONCURRENCY` и `MAIL_MAX_RETRIES` (пределы backend: от 1 до 4 workers и не более 8 повторов). Обычный retry использует exponential backoff с небольшим jitter и максимумом 15 минут. Ошибка rate limit ставит очередь на паузу примерно на час. После перезапуска процесса незавершённые `sending` jobs возвращаются в `queued`.

## 8. API

Все маршруты, кроме входа и OAuth callback, требуют действующую session cookie. POST-маршруты дополнительно требуют `X-CSRF-Token`.

Статика раздаётся по явному белому списку: наружу отдаются только шрифты и сама страница приложения. Любой другой файл проекта — исходники, миграции, `.env` — возвращает `404`.

| Метод | Маршрут | Назначение |
|---|---|---|
| `GET` | `/api/auth/me` | текущая сессия и публичные данные пользователя |
| `GET` | `/api/auth/yandex/start` | начать вход через Яндекс (сессии ещё нет) |
| `POST` | `/api/auth/login` | резервный локальный вход по паролю |
| `POST` | `/api/auth/logout` | завершение сессии |
| `GET` | `/api/dashboard/summary` | KPI и последние заявки |
| `GET` | `/api/requests` | список заявок |
| `POST` | `/api/requests` | создание заявки и позиций |
| `GET` | `/api/requests/{id}` | заявка, позиции и поставщики |
| `POST` | `/api/requests/{id}/search` | запуск поиска в фоне |
| `GET` | `/api/suppliers?request_id={id}` | список поставщиков по заявке |
| `GET` | `/api/blacklist` | чёрный список |
| `POST` | `/api/blacklist` | добавить компанию в blacklist |
| `GET` | `/api/correspondence` | список тредов |
| `GET` | `/api/mail/status` | статус подключённого ящика |
| `GET` | `/api/mail/request-status?request_id={id}` | статусы отправки по поставщикам заявки |
| `POST` | `/api/mail/test` | проверить почтовое соединение |
| `POST` | `/api/mail/send` | поставить одно письмо в очередь |
| `POST` | `/api/mail/send-bulk` | поставить отдельные письма выбранным поставщикам |
| `GET` | `/api/mail/queue` | статистика очереди |
| `POST` | `/api/mail/sync` | синхронизировать входящие |
| `GET` | `/api/mail/inbox` | неразобранные входящие |
| `GET` | `/api/mail/threads` | сообщения конкретного треда |
| `GET` | `/api/mail/yandex/start` | подключить почтовый ящик (сессия уже есть) |
| `GET` | `/oauth/yandex/callback` | общий callback: различает вход и подключение почты |

Типовые ответы:

- `200` — успешно;
- `201` — сущность создана;
- `202` — задача поставлена в фон/очередь;
- `400` — ошибка входных данных;
- `401` — нет сессии или неверный CSRF;
- `404` — сущность или маршрут не найдены;
- `429` — превышен лимит API/почтового провайдера;
- `500` — непредвиденная ошибка backend;
- `503` — временная ошибка провайдера.

## 9. Авторизация и доступ к данным

### Вход через Яндекс — основной способ

1. `/api/auth/yandex/start` начинает поток, когда сессии ещё нет: генерирует `state` и PKCE-пару, сохраняет их в `oauth_login_states` и кладёт `state` в отдельную cookie с областью действия `/oauth/yandex/callback`;
2. `/oauth/yandex/callback` — общий callback. Он сначала пробует трактовать `state` как подключение почты (требует существующую сессию), затем как вход; вход принимается только если `state` из БД совпадает с тем, что в cookie;
3. по коду выдаётся токен, из профиля берётся email, `get_or_create_oauth_user()` находит или создаёт пользователя и его рабочее пространство;
4. тот же грант сразу сохраняется как почтовый аккаунт — отдельно подключать ящик не нужно. Если шифрование токенов не настроено, вход всё равно завершается успешно, а почта остаётся неподключённой.

Пользователь, созданный через OAuth, не имеет пригодного пароля: в `password_hash` пишется хеш случайного токена, поэтому войти по паролю под таким аккаунтом нельзя.

Локальный вход по `APP_USER_EMAIL`/`APP_USER_PASSWORD` остаётся резервным для разработки и спрятан в интерфейсе под «Локальный вход для разработки».

### Общая механика сессии

1. backend создаёт случайный session token и CSRF token;
2. session token уходит в `HttpOnly` cookie `session_id` с `SameSite=Lax` (и `Secure` на HTTPS);
3. в БД сохраняются только хеши session и CSRF token;
4. session действует 8 часов;
5. каждый защищённый запрос получает `user_id` и `workspace_id` из сессии;
6. SQL-запросы дополнительно фильтруют данные по workspace.

Все изменяющие POST-запросы требуют заголовок `X-CSRF-Token`. Для POST API действует ограничение 30 запросов за 60 секунд на session token.

OAuth state живёт 10 минут и используется однократно. PKCE использует `S256`. State подключения почты дополнительно привязан к конкретной session/user/workspace; state входа привязан к cookie, так как сессии в этот момент ещё нет.

### Данные до входа

Страница приложения отдаётся сервером динамически. Без действующей сессии в неё подставляется пустой набор данных, поэтому анонимный посетитель не получает ни контактов поставщиков, ни адреса пользователя. Реальные данные попадают в ответ только вместе с валидной cookie.

Это по-прежнему не полноценная multi-tenant система: workspace создаётся автоматически на каждого нового пользователя Яндекса, ролей и приглашений нет.

## 10. Почта и OAuth

Подробная инструкция находится в [`docs/mail-integration.md`](docs/mail-integration.md). Краткая схема:

```text
Подключить Яндекс Почту
        ↓
OAuth authorization code + state + PKCE
        ↓
callback /oauth/yandex/callback
        ↓
обмен code на access/refresh token
        ↓
получение email аккаунта
        ↓
AES-256-GCM шифрование токенов
        ↓
SMTP XOAUTH2 / IMAP XOAUTH2
```

Используемые scopes:

- `mail:smtp` — отправка;
- `mail:imap_full` — чтение входящих по IMAP;
- `login:email` — email подключённого аккаунта.

Токены никогда не должны попадать во frontend, API-ответы или логи. Пароль от почты приложение не запрашивает.

### Что происходит при отправке письма

1. Frontend передаёт `request_id`, одного поставщика или массив поставщиков, тему, текст и вложения.
2. `MailService` проверяет текущую сессию, workspace, подключённый mail account, email получателя, тему, текст и вложения.
3. При необходимости просроченный access token обновляется через refresh token; новые токены снова шифруются и сохраняются.
4. Текст шаблона персонализируется переменными `{{supplier_name}}`, `{{request_name}}`, `{{request_description}}`, `{{sender_name}}`, `{{company_name}}`.
5. Репозиторий в одной транзакции создаёт/переиспользует thread, создаёт `mail_message`, сохраняет вложения, создаёт `mail_job` и переводит supplier state в `queued`.
6. Worker забирает job, переводит её в `sending` и вызывает `YandexMailProvider.send_message()`.
7. Только после успешного ответа SMTP/API message и job получают `sent`, provider Message-ID и `sent_at`.
8. При временной ошибке job возвращается в `queued`; при окончательной ошибке становится `failed`.

Таким образом, запись «Отправлено» в интерфейсе не ставится заранее: она появляется только после подтверждения провайдера.

Для production callback должен быть зарегистрирован в Яндекс OAuth буквально, например:

```text
https://your-domain.example/oauth/yandex/callback
```

## 11. Входящие сообщения

Синхронизация выполняется через IMAP XOAUTH2 и UID-курсор:

1. читается папка `INBOX`;
2. используется `BODY.PEEK[]`, чтобы не менять статус прочитанности;
3. сначала проверяются `In-Reply-To` и `References`;
4. затем используется безопасное сопоставление по теме и адресу поставщика;
5. найденное письмо добавляется в `mail_messages` как `direction=inbound`;
6. письмо без безопасной связи сохраняется в `mail_inbox_messages`;
7. повторный sync не создаёт дубликат.

### Как показывается письмо

При приёме сохраняется собственный HTML отправителя, а `text/plain` остаётся запасным вариантом. Раньше парсер выбрасывал HTML-часть и собирал «HTML» из текста, заменяя переводы строк на `<br>`, — ссылки, списки и таблицы уничтожались на входе, и никакой рендер их уже не восстанавливал.

Показ письма защищён двумя независимыми слоями:

1. `sanitize_email_html()` оставляет только разрешённые теги и атрибуты, вырезает `script`, `iframe`, `form`, `svg`, обработчики событий и атрибут `style`, а из ссылок — небезопасные схемы; внешние ссылки получают `rel="noopener noreferrer nofollow"`;
2. результат рендерится в `<iframe sandbox>` без единого разрешения и с запрещающим CSP, поэтому скрипт не выполнится, даже если проскочит мимо санитайзера.

Внешние изображения по умолчанию не загружаются, и об этом есть видимая пометка: картинка в письме — обычный способ отследить факт прочтения. Встроенные `data:`-изображения показываются.

Письма, сохранённые до этого изменения, остались с прежним испорченным HTML — исправление действует на вновь синхронизированные.

## 12. База данных и миграции

Основные таблицы:

- `users`, `workspaces`, `workspace_members`, `sessions`, `oauth_states`;
- `requests`, `request_meta`, `request_positions`;
- `suppliers`, `supplier_profiles`, `request_suppliers`, `request_supplier_states`;
- `blacklist_entries`, `audit_events`;
- `mail_accounts`, `mail_threads`, `mail_messages`, `mail_attachments`;
- `mail_jobs`, `mail_sync_states`, `mail_inbox_messages`.

Связь переписки:

```text
workspace + request + supplier → mail_thread
mail_thread → mail_messages
mail_message → mail_job (для исходящего письма)
request + supplier → request_supplier_state
```

Миграции применяются репозиторием при инициализации. Для production используется PostgreSQL через `DATABASE_URL`; для локального режима — SQLite по пути `MAIL_DB_PATH`.

На Vercel нельзя полагаться на SQLite в `/tmp` для постоянных данных: serverless-инстанс эфемерен. Production должен использовать Neon/PostgreSQL.

## 13. Деплой на Vercel

В проекте используется:

- [`api/index.py`](api/index.py) — точка входа Python Function;
- [`vercel.json`](vercel.json) — все запросы направляются в `/api/index.py`;
- `includeFiles` — HTML, шрифты и миграции попадают в deployment;
- `excludeFiles` — `.env`, тесты, docs, cache и артефакты исключаются из deployment.

Команды:

```powershell
npx vercel login
npx vercel link
npx vercel env add APP_USER_EMAIL production
npx vercel env add APP_USER_PASSWORD production
npx vercel env add DATABASE_URL production
npx vercel env add MAIL_TOKEN_ENCRYPTION_KEY production
npx vercel env add YANDEX_CLIENT_ID production
npx vercel env add YANDEX_CLIENT_SECRET production
npx vercel env add YANDEX_REDIRECT_URI production
npx vercel env add XMLRIVER_USER production
npx vercel env add XMLRIVER_KEY production
npx vercel --prod
```

Публичный callback и `APP_BASE_URL` должны указывать на один и тот же production-домен. После изменения ENV нужен новый deployment.

После деплоя проверить:

1. открывается `/supplier_finder.html`;
2. можно войти;
3. `/api/auth/me` возвращает `authenticated: true` после входа;
4. OAuth callback совпадает с зарегистрированным URI;
5. создаётся заявка;
6. поиск возвращает поставщиков;
7. письмо отправляется только после явного нажатия пользователем.

## 14. Тестирование

### Unit и integration tests

```powershell
python -m unittest discover -s tests -v
```

Тесты покрывают:

- создание заявки и сохранение нескольких позиций;
- lifecycle поиска и сохранение результата;
- state/PKCE OAuth и одноразовость state;
- шифрование токенов;
- refresh access token;
- проверку email и вложений;
- отдельные threads/messages для массовой отправки;
- повторную отправку в существующий thread;
- retry временной SMTP-ошибки;
- синхронизацию входящих и дедупликацию;
- неразобранные входящие;
- очистку CSS/HTML артефактов из письма.

### Проверка синтаксиса

```powershell
python -m py_compile supplier_app.py
node -e 'const fs=require("fs"); const html=fs.readFileSync("supplier_finder.html","utf8"); const start=html.indexOf("<script>")+8; const end=html.indexOf("</script>",start); new Function(html.slice(start,end)); console.log("JavaScript syntax: OK");'
```

### Smoke-тест сайта

Минимальная проверка:

```powershell
$page = Invoke-WebRequest -UseBasicParsing http://127.0.0.1:8000/supplier_finder.html
$page.StatusCode
```

Затем через браузер:

1. войти;
2. открыть «Мои заявки»;
3. создать заявку;
4. добавить две позиции отдельными строками;
5. запустить поиск;
6. дождаться `Завершена`;
7. проверить, что поставщики отображаются как `Не отправлен`;
8. не запускать отправку, если это только проверка.

## 15. Безопасность и операционные правила

- не хранить секреты в Git;
- не передавать OAuth-токены во frontend;
- не выводить access/refresh token в логи;
- не использовать пароль пользователя для доступа к почте;
- проверять session, workspace и CSRF на каждом изменяющем endpoint;
- раздавать статику только по белому списку: любой файл проекта, попавший в раздачу, становится публичным (так уже случалось — наружу отдавались исходники и миграции);
- не встраивать реальные данные в статическую страницу: страница приложения собирается на сервере и получает данные только при валидной сессии;
- не выводить HTML письма без санитайзера и изолирующего iframe одновременно;
- не отправлять массовую рассылку через CC/BCC;
- соблюдать лимиты XMLRiver и Яндекс.Почты;
- ограничивать retry и concurrency;
- при rate limit останавливать или замедлять очередь;
- валидировать email, размер и тип вложений;
- экранировать текст писем;
- не считать SMTP-индикацию успешной, пока провайдер не подтвердил отправку;
- перед изменением схемы добавлять отдельную миграцию, а не переписывать существующую.

## 16. Ограничения текущей версии

- **обогащение внутри одной заявки идёт последовательно**: краулинг распараллелен (5 воркеров), но веб-поиск-фоллбэк и LLM-ступень для каждого сайта — один за другим в простом цикле (`_enrich_suppliers` в `supplier_app.py`). На глубине `SERP_PAGES=10` (≈70-80 сайтов) это ощутимо — заявка может обрабатываться несколько минут, это ожидаемая цена правила «обрабатываем всё, что нашли», не зависание;
- **нет общего кэша**: выдача и результаты обхода сайтов привязаны к рабочему пространству (`UNIQUE (workspace_id, external_key)`), поэтому разные пользователи по одному ключу тратят платные запросы повторно;
- **реестр Checko используется только для сверки уже найденного ИНН**: отдельной карточки «поднять компанию по ИНН вручную» в интерфейсе нет;
- вход через Яндекс работает; локальный вход по паролю остаётся резервным для разработки;
- поиск запускается одним фоновым worker’ом, одновременно обрабатывается только одна заявка;
- входящие синхронизируются вручную/при открытии раздела, постоянного webhook/push нет;
- полноценная история папок Sent/Drafts ещё не реализована как отдельный mail client;
- Gmail и Outlook пока не подключены;
- очередь находится внутри процесса и не заменяет отдельный production worker;
- на Vercel для надёжной очереди и фоновых задач потребуется внешний durable queue/worker;
- автоматическая классификация «производитель/оптовик/розница/подрядчик» определяется по основному ОКВЭД (`classify_okved`) и **с 26 августа выводится в интерфейсе** (бейдж у названия + фильтр «Производители»); ограничение в том, что она есть только у компаний с подтверждённой записью в реестре — на 26 августа это 115 профилей из 303;
- мобильный интерфейс требует отдельного регулярного визуального регрессионного прогона;
- в экране «Поставщики» нет импорта/экспорта CSV и ручного слияния дублей-карточек — осознанно отложено, см. раздел 7 [docs/suppliers-screen.md](docs/suppliers-screen.md).

## 17. Рекомендуемый следующий этап

1. распараллелить стадию обогащения (веб-фоллбэк + LLM) по сайтам внутри одной заявки — сейчас это единственный медленный последовательный участок конвейера;
2. сделать общий кэш выдачи и компаний, чтобы разные пользователи не оплачивали один и тот же запрос дважды;
3. импорт/экспорт CSV и ручное слияние дублей на экране «Поставщики» (см. [docs/suppliers-screen.md](docs/suppliers-screen.md));
4. распараллелить/ускорить обход госзакупочного стиля «одна заявка → сотня карточек» пагинацией на бэкенде (сейчас пагинация «показать ещё» только на фронтенде, весь список всё равно приходит одним ответом API);
5. вынести отправку и входящую синхронизацию в durable queue/worker;
6. добавить полноценные миграции с версионированием и командой rollback;
7. покрыть frontend Playwright-тестами;
8. добавить Gmail и Outlook через `MailProvider`;
9. добавить фоновые расписания для IMAP sync;
10. добавить аудит действий пользователя в интерфейс;
11. проводить desktop/tablet/mobile visual regression перед каждым production deployment.

## 18. Последняя проверка проекта

### 25 августа 2026 года

Проверено вживую:

- **frontend полностью перенесён на React SPA** (`frontend/`), старый `supplier_finder.html` архивирован в `_archive/frontend-legacy/`. Пройден полный обход по реальным данным backend'а: вход, рабочий стол, заявки, поставщики, чёрный список, переписка — без единого мока/Supabase;
- **конвейер извлечения контактов подключён к живому поиску** (был написан, но не вызывался — см. запись от 23 августа ниже). Проверено на реальных, ранее не встречавшихся доменах;
- добавлена, протестирована и подтверждена вживую защита от ложной привязки чужого ИНН к сайту (`registry_owns_site`/`registry_ownership_unknown` в `verify.py`) — до фикса конвейер один раз перепутал `all-kabel.ru` с несвязанной НКО «Атлантис» по совпавшему в тексте страницы ИНН;
- краулер захардненен: ретраи на сетевых сбоях и HTTP 429 с уважением `Retry-After`, различение `blocked`/`rate_limited`/`unreachable` вместо одного «unreachable», список `stop_domains.txt` расширен и реально подключён к поиску (раньше существовал, но не читался);
- убран искусственный потолок на количество обрабатываемых сайтов — правило «сколько нашли, столько и обрабатываем» (`SERP_RESULTS_PER_POSITION` пуст по умолчанию);
- LLM-модель для extraction-ступени переведена на более дешёвую (`mistralai/mistral-nemo`), Anthropic/Claude сознательно не используется и не тестируется для этой роли (стоимость);
- **добавлен и вживую проверен веб-поиск-фоллбэк** (`web_lookup.py`, XMLRiver+Яндекс, ~2,5 коп/запрос) для сайтов, которые краулер не может достать вообще (антибот, бан по robots.txt, DNS-сбой). Прямая точечная проверка на `moscow.petrovich.ru` (заблокирован `robots.txt`) и `ozon.uz` (HTTP 403) — оба сайта, недостижимые для краулера, дали реальные email и валидный по контрольной сумме ИНН через фоллбэк. На полном боевом прогоне (заявка №1052, глубина `SERP_PAGES=10`, 79 найденных сайтов) доля сайтов с email выросла с 6/74 (прогон того же дня без фоллбэка) до **75/79 (95%)**, с ИНН — до 65/79 (82%); только 4 сайта из 79 остались без email (`gipermarketdom.ru`, `apelsin.ru`, `vashdom24.ru`, `domlenta.ru`). Полный прогон занял около 10 минут — стадия обогащения последовательна (см. раздел 16), это цена, а не побочный эффект;
- построен и вживую проверен (полный цикл чтение/запись) экран «Поставщики» — CRM-картотека по ИНН, см. [docs/suppliers-screen.md](docs/suppliers-screen.md);
- найден и исправлен попутный баг словаря статусов доставки (`_normalize_mail_status`) — раньше внутренние состояния конвейера отправки могли отображаться в интерфейсе нераспознанными;
- 31 backend-тест проходит.

Тем же днём, отдельным заходом:

- **сайты с уже известным email в рамках рабочего пространства больше не обходятся повторно** при новом поиске — `MailRepository.suppliers_with_email()` + фильтр в `_enrich_suppliers()` (`supplier_app.py`). Раньше один и тот же домен, найденный по двум разным заявкам, полностью перекраулился и повторно оплачивал веб-фоллбэк каждый раз;
- рабочая глубина `SERP_PAGES` возвращена с 10 (тестовое значение) на 3;
- в карточку поставщика добавлена компактная строка с фактами из реестра и финансовой отчётности Checko (возраст, статус, выручка, прибыль за последний отчётный год, ссылка на профиль) — `migrations/008_supplier_registry.sql` + `migrations/009_supplier_finances.sql`, см. [docs/suppliers-screen.md](docs/suppliers-screen.md) §5.1;
- **выручка и прибыль подключены по-настоящему**: проверил официальную документацию Checko (`https://checko.ru/integration/api`) — метод `/v2/finances` реален, входит в тот же бесплатный тариф, коды строк 2110/2400 — стандартные коды формы №2 бухотчётности (выручка/чистая прибыль), не выдумка. Живьём получена реальная отчётность (ООО «Строительные системы», ИНН 2312200792: выручка 490 млн ₽, прибыль 6,6 млн ₽ за 2025 год);
- **добавлена ротация ключей Checko**: при исчерпании дневного лимита одного ключа (`CHECKO_KEY`) клиент сам переключается на следующий (`CHECKO_KEY_2`, ...) — подтверждено вживую в этом же прогоне: первый ключ отдал «лимит исчерпан», клиент переключился и получил данные вторым.

### 26 августа 2026 года

Сквозная проверка сервиса и разбор данных Checko в таблицах.

Проверено вживую (полный цикл, реальные данные и реальная отправка):

- создана заявка №1053, поиск завершился 18 поставщиками, 17 с контактами;
- **отправлено настоящее письмо** через подключённый Яндекс-ящик (`status: sent`, реальный `message_id` от Яндекса), тред корректно связался с заявкой и поставщиком в `/api/correspondence`, IMAP-синхронизация отработала;
- подключён третий ключ Checko (`CHECKO_KEY_3`), ротация трёх ключей подтверждена в бою: два исчерпанных ключа последовательно уступили место третьему.

Найдено и исправлено три реальных бага:

1. **Оптимизация «не обходить сайты с известным email» заодно отсекала запрос в Checko.** Поставщик, чей email нашёлся до появления колонок реестра (или в день исчерпанной квоты), больше никогда не получал бы данные ЕГРЮЛ — обход пропущен, значит и Checko не вызывается. Добавлен отдельный дешёвый проход `SupplierApp._enrich_registry_backlog()` + `MailRepository.suppliers_missing_registry()`: без обхода, без LLM, без веб-поиска — только реестр и финансы по уже известному ИНН, с той же защитой `registry_owns_site`. Проверено на заявке №1053: `enginer-pro.ru` и `neptun66.ru` получили данные (10 лет / 312,5 млн ₽ и 11 лет / 944,2 млн ₽), а три «чужих» ИНН остались отброшенными — как и должно быть.
2. **ИП не получали ссылку на Checko.** `checko_client._fill()` читал только поле `ОГРН`, а у индивидуальных предпринимателей реестр возвращает `ОГРНИП` — поле оставалось пустым, ссылка не строилась. Исправлено чтение (`ОГРН` или `ОГРНИП`), дозаполнено для 18 ИП в базе.
3. **Адрес профиля ИП на Checko отличается.** Проверено живыми запросами: `checko.ru/company/{ОГРН}` → 200, `checko.ru/entrepreneur/{ОГРНИП}` → 200, а `checko.ru/company/{ОГРНИП}` → **404**. Ссылка теперь выбирает путь по длине номера (15 цифр = ОГРНИП = ИП, 13 = ОГРН = организация). Выборочно проверены три реальных адреса — все 200.

Вёрстка таблиц приведена к единой сетке:

- данные Checko больше не слеплены в одну строку под ИНН — у каждого показателя своя колонка: **Возраст · Выручка · ЕГРЮЛ · Checko**. Общие ячейки (`AgeCell`, `RevenueCell`, `RegistryStatusCell`, `CheckoLinkCell` в `components/suppliers/RegistryFinanceRow.tsx`) используются и таблицей заявки, и картотекой поставщиков, и чёрным списком — чтобы одно исправление чинило все три экрана сразу;
- убрана дыра между контактами и статусом: обе таблицы поставщиков переведены с «резиновых» колонок на явный `gridTemplateColumns`, общий для шапки и строк (фиксированная ширина у коротких предсказуемых фактов, `minmax()` только там, где содержимое реально разной длины);
- та же дисциплина ширин применена к таблицам заявок на `/requests` и дашборде: слack поглощает только колонка названия, числа выровнены вправо и набраны `tabular-nums`;
- в список заявок добавлена колонка **Дедлайн** (данные уже собирались с прошлого захода, но нигде не показывались); срок в пределах недели подсвечивается.

Не проверено на этот момент:

- визуальное подтверждение новой сетки скриншотом — расширение Chrome весь заход не отдавало снимки (`Debugger is not attached`); структура и данные подтверждены текстовым снимком DOM и прямыми API-запросами, но не картинкой;
- ответ поставщика на реальное отправленное письмо (на момент записи не пришёл — письмо ушло за минуты до проверки).

Продолжение того же дня — разбор «корявых названий» и ревизия проекта.

**Найден корневой баг разбора контактов Checko.** У организаций поле «Емэйл» приходит списком, у ИП — **строкой**. Код перебирал значение как список и на строке получал список отдельных букв; среди «символов» оказывался `@`, поэтому `registry_ownership_unknown()` считала, что в реестре есть посторонний рабочий адрес, и проверка принадлежности сайта отбраковывала запись целиком. Следствие: **ни один ИП не получал ни названия, ни возраста, ни статуса, ни ссылки** — в списке вместо компании оставался заголовок страницы из выдачи («Как выбрать скважинный насос…»). Исправлено helper'ом `_as_list()` в `checko_client.py`, данные дозаполнены — ИП без реестра в базе не осталось.

Прочее по тому же заходу:

- **ИП теперь подписаны как ИП**: `displaySupplierName()` (`frontend/src/lib/utils.ts`) превращает «Опрышко Светлана Александровна» в «ИП Опрышко С. А.». ИНН из 12 цифр — признак физлица, из 10 — организации;
- **добавлена колонка «Прибыль»** — компонент `ProfitCell` был написан ранее, но нигде не подключён (убыток показывается красным со стрелкой вниз);
- **сайт поставщика открывается прямо из строки** (`SiteLink`, `stopPropagation` не даёт строке одновременно открыть боковую карточку);
- **название компании берётся только при подтверждённой записи в реестре.** Раньше признаком служил сам факт наличия ИНН — но ИНН может принадлежать маркетплейсу, чью страницу мы обошли (`am.ozon.com` несёт ИНН ООО «Интернет Решения»), и тогда в названии оставался заголовок товара. Без подтверждения честнее показать домен;
- **реализовано ранее собираемое, но никогда не показываемое**: роль в цепочке поставок (`производитель`/`оптовик`/`розница`/`подрядчик`, определяется по основному ОКВЭД в `classify_okved`) и регион. Роль выводится бейджем у названия — «производитель» подсвечен, потому что для закупщика это главное отличие завода от перекупщика; регион — в колонке контактов. Добавлен фильтр «Производители» в тулбар заявки. До этого захода 115 записей роли и 121 региона лежали в базе и не были видны нигде;
- **укреплена раздача статики**: путь, похожий на исходник или конфиг (`.py`, `.sql`, `.env`, `.sqlite3`, `.key`, …), теперь получает честный 404 вместо оболочки SPA. Утечки не было и раньше — отдавался `index.html`, — но статус 200 на `/.env` выглядел как существующий файл для сканера и замаскировал бы будущую ошибку в белом списке. Проверено: пять таких путей отдают 404, все SPA-маршруты по-прежнему 200.

**Проверка здоровья системы (26 августа):** 13 API-эндпоинтов и 10 SPA-маршрутов — все 200, проблем 0. CSRF без токена → 403, аноним к `/api/requests` → 401, чёрный список без причины → 400, исходники и `.env` → 404. 31 backend-тест, `tsc` и сборка — чисто.

Отдельным заходом в тот же день, по прямым замечаниям владельца проекта:

- **исправлена дангл-ссылка на удалённый `/supplier_finder.html`** во всех редиректах OAuth-входа через Яндекс (`supplier_app.py`, 6 мест) — после миграции на React SPA они указывали на архивированный файл. Логин теперь редиректит на `/login?error=...` (страница читает и показывает код ошибки) и `/` при успехе; подключение почты — на `/settings?...` (экран `/settings` построен в тот же день, см. запись ниже про дефектную ведомость, §S-03);
- добавлен **список позиций заявки** на страницу `/requests/:id` — раньше `state.detail.positions` загружались, но нигде не отображались, только счётчик «N позиций» в шапке;
- построен **экран «Чёрный список»** (`/blacklist`) поверх общей таблицы с `/suppliers`, с обязательной причиной блокировки — подробности в [docs/suppliers-screen.md](docs/suppliers-screen.md) §6.1;
- зафиксировано в памяти agent'а правило: перед тем как заявлять «невозможно» про стороннюю интеграцию, сверяться с официальной документацией провайдера, а не только с текущим кодом — конкретный повод: выручка/прибыль Checko (раздел выше) сначала были ошибочно названы недоступными;
- зафиксировано отдельное правило по визуальному стилю — без нео-градиентов, глассморфизма, дефолтных теней и шаблонных сеток из карточек. Первое найденное и исправленное нарушение — экран входа: убран WebGL-фон «MagicRings» с синими/фиолетовыми conic-градиентами и `backdrop-blur` панели, заменено на плоский дизайн в палитре остального приложения (заодно упало на 460&nbsp;КБ собранный бандл — из него ушёл `three.js`, который был нужен только этому фону);
- из формы новой заявки и списка позиций убрано поле «Количество» — по прямому указанию владельца проекта («только ключи»), это же снимает пункты R-04/R-05 из дефектной ведомости о неровной вёрстке и путанице у поля количества;
- ссылка на профиль Checko перекрашена в оранжевый и получила иконку `BadgeCheck` — точный логотип Checko не подтверждён (сеть до статических файлов checko.ru недоступна прямо сейчас), использован нейтральный значок. Отдельно: собственный `theme-color` Checko — зелёный (`#5fac62`), не оранжевый, для сведения.

Не проверено на этот момент:

- поведение стадии обогащения при нескольких одновременных заявках (сейчас конвейер обрабатывает по одной заявке, стадия обогащения внутри неё последовательна — см. раздел 16);
- обнаружение bounce на реальном почтовом трафике (только на синтетических примерах, см. [docs/suppliers-screen.md](docs/suppliers-screen.md));
- поведение картотеки поставщиков при количестве карточек на порядок больше текущего;
- реальная ссылка на профиль компании на checko.ru (`https://checko.ru/company/{огрн}`) — подтверждён только API-эндпоинт `/integration/api`, не сама публичная страница профиля;
- визуальный вид встроенной мини-формы причины блокировки на карточке поставщика — backend-логика проверена напрямую через API (400 без причины, 200 с причиной, корректная очистка при восстановлении), но клик по интерфейсу не удалось воспроизвести в браузере из-за сбоя инструмента (0×0 viewport, ошибка `clip.scale` у скриншотов) — не связано с кодом приложения;
- точная величина свободного горизонтального поля у таблиц на широких экранах — не измерено вживую (тот же сбой инструмента), но по CSS все списковые страницы используют `max-w-[1440px] mx-auto` при боковой панели 248px, то есть на экранах шире ≈1690px по бокам таблицы остаётся неиспользуемое поле.

### 25 августа 2026 года — дефектная ведомость (41 пункт)

Владелец проекта провёл ручной обход всех экранов рабочей сборки и передал дефектную ведомость (`ДефектнаяведомостьSupplyDesk.html`, id S/D/R/C/P/M/B) на 5 блокеров, 16 крупных и 20 средних замечаний. Разобрано по её же плану («Заходы» 1–4), каждый заход — рабочий `npm run typecheck` + `npm run build` + 31 backend-тест перед переходом к следующему. Пункт за пунктом:

- **S-06** — `lang="ru"` в `frontend/index.html` (было `en`), заодно убраны неиспользуемые og/twitter-теги от шаблона Bolt.new (указывали на `bolt.new/static/...`);
- **C-03 (реальный баг backend, не только текста)** — `MailRepository.get_request()` делал `SELECT * FROM requests`, то есть отдавал только сырые колонки таблицы: `status`, `search_progress`, `search_total`, `positions_count` и другие вычисляемые поля были `undefined` на странице конкретной заявки (хотя корректно приходили в списке заявок). Из-за этого не только не подставлялось число перед «позиций», но и подсветка текущего шага воронки («Заявка → Поиск → …») не работала вообще. Исправлено переиспользованием одного SQL-фрагмента в `list_requests()` и `get_request()`;
- **S-05** — контраст поднят до ≥4,5:1 на цитируемых местах (заголовки таблиц `ink-400→ink-600` и 10→11px, подписи сайдбара `ink-400→ink-500`, подпись KPI на синей плитке — до чистого белого);
- **S-10** — общий `pluralize()` (`frontend/src/lib/utils.ts`) применён во всех счётчиках, которые ведомость поймала на неправильной форме («79 поставщика», «2 писем» и т.д.);
- **S-07** — кнопка «Выйти»: область нажатия до 40×40, добавлены `title`/`aria-label`;
- **D-04, D-05** — убрана нерабочая иконка часов у кнопки «Обновить»; заголовок дашборда приведён к «Дашборд» (совпадает с сайдбаром), убрана дублирующая подпись-«глазок» на дашборде и на «Заявках» (там же сайдбар переименован в «Мои заявки» для согласованности с хлебными крошками);
- **C-01 (блокер)** — `frontend/src/components/Composer.tsx` переписан: список получателей с адресами и чекбоксами на исключение, строка «Отправитель: …» (`GET /api/mail/status`), предпросмотр письма для первого получателя, обязательное подтверждение («Отправить N писем? Отменить будет нельзя») при получателях больше одного;
- **C-05** — плавающая панель выбора теперь скрывается, пока открыто окно отправки (`{!composerOpen && <StickyToolbar .../>}`), а не полагается на сравнение z-index;
- **C-04** — чекбоксы увеличены до 24px с областью нажатия 40px в обеих таблицах поставщиков; боковая панель поставщика закрывается по <kbd>Esc</kbd>;
- **D-01 (реальный баг данных, не только текста)** — KPI «Новые ответы» считал все входящие письма когда-либо, включая непривязанные (рассылки/уведомления из вкладки «Без привязки»), и не мог уменьшиться. Добавлена таблица `mail_message_reads` (`migrations/011_message_read_state.sql`) — открытие треда помечает его входящие письма прочитанными (`thread_messages()`), счётчик в `dashboard_summary()` считает только непрочитанные входящие из привязанных к заявке тредов. Заодно (D-02) все 4 плитки KPI стали кликабельными ссылками на соответствующий отфильтрованный список и окрашиваются только при ненулевом значении;
- **S-01 (блокер)** — мобильная навигация: `frontend/src/components/Layout.tsx` получил мобильную шапку с гамбургер-меню и выезжающий drawer-сайдбар ниже брейкпоинта `lg`;
- **S-02 (блокер)** — картотека поставщиков (`GlobalSupplierTable.tsx`) обёрнута в `overflow-x-auto` с `min-w-[900px]`, чтобы 12-колоночная сетка не схлопывалась на узком экране. Таблица поставщиков заявки (`SupplierTable.tsx`) уже адаптировалась иначе (скрытие второстепенных колонок по брейкпоинтам) — оставлено как есть, это тоже рабочая стратегия, не тот же дефект;
- **S-03 (блокер)** — построен экран `/settings` (`frontend/src/pages/Settings.tsx`): подключённый адрес, кнопки проверки соединения/синхронизации/отключения, подключение Яндекс.Почты, если ещё не подключена. Пункт «Настройки» в сайдбаре теперь активен;
- **S-04 (блокер)** — экран 404 (`frontend/src/pages/NotFound.tsx`) вместо тихого редиректа на дашборд для любого нераспознанного пути;
- **C-02 (частично)** — на карточку заявки возвращены бейдж статуса и полоса прогресса поиска (используют те же поля, что чинил C-03). **Не сделано**: кнопка «Остановить поиск» — в `_run_search`/`_enrich_suppliers` нет механизма отмены (простой daemon-поток без cancellation token), кнопка без реальной отмены была бы обманом интерфейса, не построена. Прогресс-бар также не детализирует стадию обогащения (краулинг/Checko/веб-фоллбэк после сбора SERP) — только сбор позиций, самая долгая часть прогона всё ещё не показывает продвижение;
- **C-06, P-03** — фильтры заявки прилипают к верху при прокрутке (`ListToolbar.tsx`); в обеих длинных таблицах (картотека поставщиков и список поставщиков заявки) добавлена постраничная подгрузка «Показать ещё» по 50 строк вместо рендера сотен строк разом;
- **P-01** — колонка «Отвечает %» разделена по смыслу: процент (`nowrap`) отдельно от статуса «ждём» под ним, заголовок сокращён до «Отклик» без переноса; заодно колонка «Всего» переименована в «Заявок» (см. P-03);
- **P-02** — фильтр «Никогда не отвечали» был равен всей базе (`total_requests===0 OR response_rate===0`, то есть «не писали» ИЛИ «писали, не ответили» в одной куче). Разделён на «Не отвечают» (писали, ответа нет) и новый «Не контактировали» (не писали вообще);
- **S-08, C-07** — общий `shortCompanyName()` (`ОБЩЕСТВО С ОГРАНИЧЕННОЙ ОТВЕТСТВЕННОСТЬЮ …` → `ООО …` и аналоги для АО/ЗАО/ОАО/ПАО/ИП) с полным названием в `title`, применён в обеих карточках поставщика и в обеих таблицах. В списке поставщиков заявки (`SupplierTable.tsx`) домен стал основным именем строки, когда ИНН не найден (то есть имя — вероятно необработанный заголовок страницы из выдачи, не подтверждённое название компании), полное название — во всплывающей подсказке;
- **C-08** — бейдж «Email ✓» получил `nowrap` (не рвёт высоту строки); колонка позиций в таблице заявки скрывается целиком, если у заявки одна позиция (незачем повторять один и тот же тег 79 раз); статус «Не отправлен» показывается обычным текстом, а не серой пилюлей — пилюли остались только для значимых статусов;
- **C-09** — боковая панель поставщика заявки (`components/SupplierPanel.tsx`): затемнение фона усилено (`/10 → /30`), контент больше не прибит к верху с пустым низом (убран `mt-auto`, кнопки идут сразу за контентом), добавлена ссылка «Карточка компании» на `/suppliers?search=<инн>` (для чего `Suppliers.tsx` научился читать `?search=` из URL), сниппет «Почему найден» очищается от висящих «…» в конце;
- **P-04** — пустые плитки «Средняя оценка сделки»/«Среднее время ответа» в карточке поставщика скрываются целиком, если данных нет, вместо прочерка на половину карточки;
- **P-05** — в карточку поставщика добавлены «Написать» (`mailto:`, кнопка неактивна без email) и «В заявку» как первичные действия сразу под шапкой; «Сообщить о проблеме» перенесено вниз нейтральной ссылкой; сайт в контактах стал кликabельной ссылкой; переключателю статуса отношений сужены отступы, чтобы «Чёрный список» не переносился на две строки;
- **M-01** — шапка «Переписки» приведена к общей высоте/отступам остальных экранов; нерабочая (`onOpenComposer={() => {}}` — кнопка ничего не делала) кнопка «Написать» в списке тредов убрана целиком, а не просто перекрашена — рисовать рабочей кнопку, которая ничего не делает, хуже, чем её не показывать;
- **M-02** — поиск появился и во вкладке «Без привязки» (`Messages.tsx`, раньше был только в «По заявкам»);
- **M-04, M-05** — при свёрнутом входящем письме под именем отправителя показывается первая строка текста вместо пустой строки; статус исходящего письма показывается словом (в очереди/отправляется/отправлено/ошибка) у самого сообщения, а не только скобками из технического кода; кнопка «Ответить» продублирована прямо под последним сообщением треда (была только в шапке, далеко от текста);
- **R-01, R-02** — единый формат даты («сегодня 14:02» / «вчера» / «23 авг», точная дата — в `title`) в списке заявок; числовые колонки (Позиций/Поставщиков/Ответов) выровнены по правому краю с `tabular-nums`, ужаты по содержимому;
- **R-04, R-05, «только ключи»** — поле «Количество» убрано из формы заявки и списка позиций по прямому указанию владельца проекта — снимает оба пункта разом (неровная вёрстка второй строки и путаница «участвует ли количество в поиске»), а не просто чинит вёрстку вокруг него.

Сознательно не сделано в этом заходе (отдельные, более крупные куски работы, не микс с текущим):
- **R-03** — переименование/дублирование/архивирование заявки из списка: нужны новые backend-эндпоинты (сейчас нет ни одного), не только правка фронтенда;
- **M-03** — квот-сворачивание уже реализовано на бэкенде (`mail/content.py:collapse_quoted_html/collapse_quoted_text`, через `quotequail`, в `<details>`) и подключено в `thread_messages()` — то есть это не отсутствующая функция, а эвристика `quotequail`, которая не распознала конкретную стену `>`-цитат, которую увидел аудитор (вероятно, сырой текстовый quote внутри HTML без стандартных маркеров типа `blockquote`/`gmail_quote`). Не патчилось глубже в рамках этой сессии;
- **M-06** — плейсхолдер «Внешние картинки скрыты · Показать» вместо пустых провалов на месте заблокированных изображений — не реализовано;
- **B-01** — уже частично снято переделкой экрана «Чёрный список» в этом же дне (объяснение в баннере и в пустом состоянии, что попадание сюда автоматическое) — кнопка «Добавить по ИНН» сознательно не добавлена: противоречит отдельно зафиксированному дизайн-решению «GlobalSupplier — производная от обогащения, не вводится вручную» (см. [docs/suppliers-screen.md](docs/suppliers-screen.md) §1);
- полноценная карточная вёрстка таблиц для мобильных экранов (вместо горизонтального скролла) — сделан более простой и безопасный вариант (S-02), не переписывание таблиц под карточки;
- перекраска экрана «Переписка»/`ThreadDetail`/`Composer`(mail) с сырых `gray-*` классов Tailwind на токены `ink-*`/`accent-*` проекта — только точечные правки (цвет кнопки, статусы), не сплошная перетекстовка всего файла.

Не проверено вживую в браузере в рамках этого захода: расширение Claude in Chrome было отключено весь заход (не связано с кодом) — все правки проверены `tsc --noEmit`, `vite build` и 31 backend-тестом, часть backend-логики (обязательная причина блокировки, счётчик непрочитанных ответов) — напрямую через API, но ни один экран не был кликнут вживую в браузере после переделки.

### 23 августа 2026 года

Проверено вживую:

- вход через Яндекс пройден до конца на локальном приложении OAuth; сессия создана, почта подключилась тем же грантом;
- без сессии `/supplier_finder.html` отдаёт пустой набор данных, `/api/auth/me` — `authenticated: false`, а `supplier_app.py`, `mail/repository.py`, `migrations/*` и `.env` возвращают `404` — на боевом домене тоже;
- модули извлечения работают из CLI: `rucabel.ru` → `zakaz@rucabel.ru` + ИНН `1327035444`, `etm.ru` → 6 адресов + ИНН `7842224734`;
- веб-поиск контакты **не** извлекает: у заявок №1044 и №1045 ноль ИНН и ноль телефонов, тогда как у fixture-заявки №1043 они есть;
- 26 автотестов проходят.

Отдельно зафиксировано в аудите интерфейса: KPI «Новые ответы» считает все входящие письма, включая не относящиеся к поставщикам, и никогда не уменьшается.

### 22 августа 2026 года

Локально через сайт была проверена заявка №1044:

- создана заявка с двумя отдельными позициями;
- поиск через XMLRiver завершился успешно;
- найдено 19 поставщиков;
- отправлено 0 писем;
- все найденные поставщики оставлены в статусе `Не отправлен`;
- интерфейс показал «Готово к отправке запросов».

Эта проверка подтверждает базовый путь заявки и поиска, но не заменяет отдельную проверку реальной OAuth-авторизации и тестовой отправки письма.
